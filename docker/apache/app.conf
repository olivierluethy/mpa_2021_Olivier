# Allow the app's .htaccess (RewriteRule for the front controller) to take effect.
<Directory /var/www/html>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>

# Quieten the "could not reliably determine the server's fully qualified domain name" warning.
ServerName localhost
